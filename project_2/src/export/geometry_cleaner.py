# The final SVG file should not have overlapping components
# (otherwise, the 3D-Printer could have problems)

# If we have an SVG-File with this property,
# we could calculate the exact volume (weight) of our design.
